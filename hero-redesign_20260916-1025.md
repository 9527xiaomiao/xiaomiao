# Hero 首屏重做 —— 参考 Annie Xie 设计语言

> 时间：2026-09-16 10:25
> 目标：把首屏排版从「左标题 + 右侧缩略」改成参考图的双列 + 身份卡 + 状态条设计语言

## 参考图（用户提供的 Annie Xie 首屏）关键视觉信号

1. 顶部 Nav：logo + 名字带 `v2026` + 7 个导航项（HOME 加 pill 高亮）+ 右侧 `● SOUND ON` + `CN/EN` 切换 + `TALK TO ANNIE` 主 CTA
2. Nav 下方的细分隔线
3. 左侧竖排小字（旋转 90°）：`BUILDING WITH AI · THINKING WITH STORIES`
4. 左主区：状态圆点 + 中英文姓名 → mono 小标 tag → 3 行超大标题（实心 / 镂空斜体 / 紫渐变）→ 副标 → 小 mono 说明 → CTA
5. 右侧身份卡：4:5 比例肖像 + 四角装饰 + 浮动 `TAP TO TALK` pill + 圆形 AI 旋转徽章 + 左下 `ANNIE AI · ONLINE`
6. 底部 bar：左 `SCROLL TO ROOT · ▾` + 右状态胶囊行（`● SYSTEM ONLINE` 等 4 个）
7. 整屏暗紫调，主色 `#a48bff` / `#c4b8ff`

## 实施细节

### 修改文件（4 个）
- `src/content.js` — profile 增字段（verticalText / statusLine / hero.lines 数组 / identity / statusBar），nav 改为对象结构（brand + links + soundOn + lang + langOptions + cta）
- `src/components/Nav.jsx` — 适配 nav 新结构，加 SOUND ON/OFF 切换、CN/EN 切换、active 项改为 pill 高亮
- `src/components/Hero.jsx` — 完全重写：双列布局 + 竖排 vtext + 状态行 + 3 行可配置大标题 + 身份卡（Media 占位图 + 浮动按钮 + 圆形 AI 徽章 + 在线胶囊）
- `src/styles.css` — 重写 hero 区（section 6）+ 新增 status-dot / status-pill / identity-* 系列样式；nav 改动（active pill、sound/lang/right 区、brand ver）

### 大标题 3 行可配置
```js
profile.hero.lines = [
  { text: '视觉', style: 'fill' },              // 实心白
  { text: 'AI',   style: 'outline-italic' },    // 镂空斜体（-webkit-text-stroke）
  { text: '设计', style: 'accent' },            // 紫色渐变（#b9a8ff → #8b7cff → #c9b8ff）
]
```
每行用 `@keyframes riseIn` 从 112% 上滑入场，依次延迟 0 / 0.12s / 0.24s。

### 身份卡核心
- Media 占位（tone={2}）+ 内边框 + 四角装饰线（accent 色，drop-shadow 发光）
- `TAP TO TALK` pill：右上角外凸，blur(10px) 半透明背景
- 圆形 `AI` 徽章：右下角外凸，conic-gradient 旋转环 + 中心渐变球 + 底部 `AGENT` 小标签
- `林墨 AI · ONLINE` 胶囊：左下，blur 半透明

### 工具 / 编码规则
- 文本写入全部走 `qclaw-text-file` 脚本（4 个文件先 write 到 `$env:TEMP\_tw_*.txt`，再 `python write_file.py --content-file ... --path ...` 落盘，platform=windows 不传 --platform）
- 全部 status ok / encoding utf-8 / bom false / newline crlf
- 已清理临时文件

## 验证

| 检查 | 结果 |
|---|---|
| `GET /` | 200 |
| `/src/components/Hero.jsx` | 200，含 `identity__badge` / `identity__portrait` |
| `/src/components/Nav.jsx` | 200 |
| `/src/content.js` | 200 |
| `/src/styles.css` | 200，含 `hero__vtext`、`hero__title-line--outline-italic`、`hero__title-line--accent`、`identity__badge-ring`、`nav__lang`、`status-pill` |
| Vite HMR | 4 次 clean update，无 error/warn |
| `npm run build` | 42 modules，830 ms，CSS 29.85 kB（gzip 6.53），JS 162.54 kB（gzip 53.90） |

## 当前状态
- Dev server 仍跑在 `http://localhost:5173/`（pid 20748，session rapid-wharf）
- 视觉 QA 仍受 xbrowser 配置阻塞（用户需选 quick / custom）
- 内容仍为占位（林墨 / hello@linmo.design），等待用户提供真实简历 / 头像 / 项目素材

## 下一步（用户回复后执行）
1. 用户选定 xbrowser 配置 → 截图做视觉 QA（首屏两列节奏、身份卡层次、标题字体镂空效果）
2. 用户提供简历 / 真实肖像 / 项目图 → 替换 `src/content.js` 对应字段，把肖像丢进 `public/media/portrait.jpg`
3. 进一步按用户新截图 / 参考站迭代

## 涉及变更的代码区（供回看）
- `src/content.js`：profile + nav 全部重排
- `src/components/Hero.jsx`：54 行 → ~110 行
- `src/components/Nav.jsx`：保持行数，新增 sound/lang/cta 节点
- `src/styles.css`：约 30 KB → 38.9 KB（+6.9 KB 全是新 hero/nav 样式）